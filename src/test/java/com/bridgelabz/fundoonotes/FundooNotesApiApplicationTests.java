package com.bridgelabz.fundoonotes;

//@SpringBootTest
class FundooNotesApiApplicationTests {

	/*@Autowired
	private UserRepository repo;

	@Test
	public void userTestCases() {
		Long id = (long) 3;
		User newUser = new User(id, "ChandrakalaVK", "chandrakala", "8147267142", "chandrakalakirasur@gmail.com", false, null,
				null, null);
		repo.save(newUser);
		UserServiceImpl userServ = new UserServiceImpl(repo);
		User fetchedUser = userServ.findAllUser().get(0);
		assertEquals(newUser.getUserName(), fetchedUser.getUserName());
	}*/

}
